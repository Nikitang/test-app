import { StartForm } from 'entities/StartForm';

const MainPage = () => {
    return (
        <div className="main-page">
            <StartForm />
        </div>
    );
};

export default MainPage;
